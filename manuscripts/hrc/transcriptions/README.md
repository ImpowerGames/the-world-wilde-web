# hrc/transcriptions

One file per transcription, named `<archive>-<item>-<first image number>.json` — so `hrc-2568-013.json` is folder 2568, image 13, at the Ransom Center. It sorts in folder-then-sheet order. The name is a local handle; the identity is the `letter_id` inside.
