__pycache__/
*.pyc
.DS_Store
layout-baseline.json

# generated by `validate.py --ledger`; rebuildable from data/
audits/

# generated by `validate.py` from the per-entity files under data/; built and published
# by .github/workflows/pages.yml, so it is never committed. Run the validator (or
# tools/serve.py, which runs it for you) before opening the site locally.
data/web.json
